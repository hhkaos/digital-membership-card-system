{
  "name": "socios-ampa",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "workspaces": [
    "verification",
    "issuer"
  ],
  "scripts": {
    "pretest": "npm install",
    "test": "npm -w verification test && npm -w issuer test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
